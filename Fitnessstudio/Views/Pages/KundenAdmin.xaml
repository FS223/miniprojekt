<Page x:Class="Fitnessstudio.Views.Pages.KundenAdmin"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Fitnessstudio.Views.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="KundenAdmin">
    <StackPanel>
        <StackPanel Orientation="Horizontal" Margin="5">
            <ComboBox Width="150" Margin="0, 0, 10, 0">
                <ComboBoxItem Content="Option 1"/>
                <ComboBoxItem Content="Option 2"/>
                <ComboBoxItem Content="Option 3"/>
            </ComboBox>
            <ComboBox Width="150" Margin="0, 0, 330, 0">
                <ComboBoxItem Content="Option 1"/>
                <ComboBoxItem Content="Option 2"/>
                <ComboBoxItem Content="Option 3"/>
            </ComboBox>
            <Button Content="Neuen Kunden hinzufügen" Width="150" HorizontalAlignment="Right" Click="Button_NeuerKunde"/>
        </StackPanel>        
        <Grid>
            <DataGrid ItemsSource="{Binding Items}" AutoGenerateColumns="False" SelectionMode="Single" SelectionUnit="FullRow">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="-" Binding="{Binding Id}" />
                    <DataGridTextColumn Header="Vorname" Binding="{Binding Vorname}" />
                    <DataGridTextColumn Header="Nachname" Binding="{Binding Nachname}" />
                    <DataGridTextColumn Header="Geburtsdatum" Binding="{Binding Geburtsdatum}" />
                    <DataGridTextColumn Header="Geschlecht" Binding="{Binding Geschlecht}" />
                    <DataGridTextColumn Header="AnschriftId" Binding="{Binding AnschriftId}" />
                </DataGrid.Columns>

                
            </DataGrid>
        </Grid>
    </StackPanel>    
</Page>
